# Persistence and Data Model

Status: **draft**

Shapes: entity fields are authoritative in `../../franz/api/franz/v1/*.proto`;
this document owns **how they are stored**.

## Concepts

Franz's only datastore is **PostgreSQL**. It holds the *declared* state of every
fleet resource plus the audit and telemetry records Franz receives. It does **not**
hold Kafka's own state (that lives in the real clusters) or generated code.

The database is reached only through
`franz/pkg/franz/adapters/out/postgres/`, which implements the `core/ports/out`
repository interfaces (`../002-monorepo-structure/`). `core/domain` and
`core/usecases` contain no SQL and no driver imports.

## Storage choices (cross-cutting)

- **One row per resource**, surrogate primary key `id uuid` (v7, time-ordered).
  The domain identity is `(realm_id, name)` — a `UNIQUE` constraint — and `frn` is
  a second `UNIQUE` column, set once at insert (`003.1`).
- **The `frn` column stores the FRN *without* its prefix** — the bare
  `<realm-slug>:<resource-type>:<name>` path. The configurable `frn`/`orn`/custom
  prefix (`003.1`) is a rendering concern applied at the API boundary, never
  persisted, so changing `resource_prefix` never rewrites stored rows.
- **`name` is never reused.** The `(realm_id, name)` constraint is unconditional;
  a soft-deleted row keeps its name and FRN.
- **Enums are `text` + `CHECK`**, not PostgreSQL `enum` types — the proto enum is
  the source of truth and a text column evolves without a migration. Stored as the
  proto short form (`ACTIVE`, `PAUSED`, `READY`, …).
- **Timestamps are `timestamptz`.** `created_at` / `updated_at` on every table.
- **Maps → `jsonb`.** `labels`, `cluster_configuration`, `topic_configuration`
  are `jsonb` objects with a **GIN index** so label-selector filtering (`003.1`)
  can push down to Postgres.
- **Repeated / document fields → `jsonb`.** `connection_strings` (array),
  `access_policy` (one document, replaced wholesale by `SetAccessPolicy`),
  `actions` lists.
- **Realm scoping.** Every table carries `realm_id` (FK → `realm`); every query
  filters on it. There is no cross-realm read path.
- **Soft delete.** `state = 'DELETED'` rows are retained. Repositories exclude
  them by default; a direct `Get` still returns one so the caller can raise
  `FAILED_PRECONDITION` (`003.11`).

## Tables

| Entity (proto) | Table | Notable storage decisions |
|---|---|---|
| Realm *(no proto)* | `realm` | `id`, `slug` (the `<realm>` in an FRN), `name`. Provisioning out of scope (`003.1`). |
| `KafkaCluster` | `kafka_cluster` | `connection_strings jsonb`, `labels jsonb`, `cluster_configuration jsonb`, `cluster_provider_agent text` (unvalidated, **no FK** — `003.3`), `state text`. |
| `AsyncChannel` | `async_channel` | `labels jsonb`, `access_policy jsonb` (whole document), `channel_partitions int`, `type text`, `state text`. |
| `KafkaTopic` | `kafka_topic` | `async_channel_id` FK, `kafka_cluster_id` FK **nullable** (unplaced shard — `003.7`), `topic_configuration jsonb` (override layer), `materialized_configuration jsonb` (frozen merge — see below), `partitions int`, `replication_factor int`, `state text`, `consumption text`, `traffic_share_value numeric` + `traffic_share_unit text`, `generation bigint`. |
| `Agent` | `agent` | `labels jsonb`, `type text`, `status text`, `token_hash text` (sha256 of the one-time bearer token — `003.9`; the plaintext is never stored). |
| `Client` | `client` | `labels jsonb`. No `state` column (`003.10`); FRN reservation on delete is an open question. |
| `Policy` | `policy` | `matcher jsonb`, `limit_operator text` + `limit_value text`, `actions jsonb`, `weight int`, `enabled bool`, `last_fired_at timestamptz`. |
| `Indicator` | `indicator` | `unit text`, `applies_to text`, `source_agents jsonb`, `health text`, `staleness_threshold text`, `last_sample_at timestamptz`. **Admin-created** — no auto-create from samples (`003.14`). |
| *(telemetry ingest)* | `indicator_sample` | **Append-only time series** (`003.14`): `(indicator, resource_frn, resource_entity, value text, sample_at, received_at)`. Index `(indicator, resource_frn, sample_at desc)`. Pruned nightly (30d). "Current" = greatest `sample_at` per pair (or an `indicator_latest` projection). |
| *(telemetry ingest)* | `observed_consumer_group` | **Append-only time series** (`003.14`): `(group_name, kafka_topic_id, async_channel_id, client_frn, custom bool, reported_by_agent, observed_at)`. Pruned nightly (30d). "Current" view = distinct `(group, topic)` with the latest `observed_at`. |
| `PolicyAction` | `policy_action` | **Append-only** audit: `policy_id` FK, `occurred_at`, `resource_frn`, `indicator_value text`, `action jsonb`, `result text`. |
| *(cluster provider)* | `cluster_provider_event` | **Append-only** (`004` ADR §4): `kafka_cluster_id` FK, `cluster_frn`, `phase text` + CHECK, `reachable bool`, `message`, `reporting_agent`, `recipe_ref`, `occurred_at`, `received_at`. Index `(kafka_cluster_id, occurred_at desc, id desc)`. "Current provider status" = newest row per cluster; surfaced as `KafkaCluster.provider_status`. Pruned nightly (30d). |
| *(migration)* | `shard_migration` | In-flight moves (`003.13`): `kafka_topic_id` FK, `source_cluster`, `target_cluster`, `phase text`, `reason text`, `started_at`, `drain_deadline`. |

**Derived views** — `ChannelClientAccess` / `ClientChannelAccess` (`003.5`,
`003.10`) are **not tables**. The usecase loads the channel `access_policy` and
the candidate `client` rows and evaluates the policy in Go (wildcard and selector
matching live in the domain).

## Materialized topic configuration

`003.3` / `003.6`: editing `cluster_configuration` affects only *future* shards;
existing shards keep the configuration they were created with. So `kafka_topic`
stores two columns:

- `topic_configuration` — the per-shard override (proto
  `KafkaTopic.topic_configuration`).
- `materialized_configuration` — `cluster_configuration` ⊕ `topic_configuration`,
  computed and **frozen** when the shard is created or its desired state changes.
  A later `cluster_configuration` edit does not touch it. This is what an agent
  reconciles against.

## Concurrency

- **Resource updates** take a row lock for the request: one transaction does
  `SELECT … FOR UPDATE`, applies the FieldMask + validates invariants in Go, then
  `UPDATE`, then `COMMIT`. No version column, no client-supplied token; the last
  committed write wins across separate requests.
- **`kafka_topic.generation`** is the separate domain token (`003.6`), bumped on
  every desired-state change; agents echo it so Franz rejects stale reports.
- **Compound operations run in one transaction** — `CreateAsyncChannel` inserts
  the channel, its `channel_partitions` `kafka_topic` rows, and the placement
  result atomically.

## Access from Go

The adapter is **hand-written `pgx/v5`** — no ORM, no query-generator. Each
repository holds its SQL as string literals with a small shared row-scan helper.
Dynamic `List*` filtering (the `003.1` selector grammar) is built as parameterised
`WHERE` fragments in Go.

## Migrations

**Flyway**, `franz/migrations/`. While Franz is pre-production a single
`V1__init.sql` is edited in place — no new versioned files until the schema is
frozen. It seeds one `realm` row (`slug = 'default'`, fixed uuid
`00000000-0000-0000-0000-000000000001`); a context resolver returns it for every
request until auth (`003.2`) carries the realm. `pkg/gen/go` is generated, not
migrated.

Franz also **applies the embedded migrations on boot** when `db.auto_migrate` is
set (default on) — `go run ./cmd/franz` against a fresh database just works.
Every statement in `V1__init.sql` is written idempotently (`… IF NOT EXISTS`,
`INSERT … ON CONFLICT DO NOTHING`) so the boot runner and the docker-compose
Flyway job cannot corrupt each other. Set `db.auto_migrate: false` wherever
Flyway owns schema changes.

## Open questions

1. **Client deletion** — a `deleted_client_frn` reservation ledger vs. adding a
   `state` / `DELETED` column to `client` (`003.10` / `003.11`).
2. **Selector push-down** — how much of the `003.1` grammar (`IN`, `NOT IN`,
   `!key`, `*` glob) becomes SQL / `jsonb` operators vs. a Go filter after a
   coarse GIN prefilter.
3. **`indicator_sample` volume** — 30d of `indicators × resources × sample rate`
   in Postgres; whether it needs partitioning (by day), a tighter retention, or an
   external TSDB with only "current" kept in Franz.
4. **`indicator_latest` projection** — maintain a separate current-value table/row
   vs. always `ORDER BY sample_at DESC LIMIT 1` off the index.
